/* ================================================================
 *  GPU_HYPO_SMALL.CUH — Constant memory + CHECK macro
 *  كل kernels موجودة في kernels_code.cu
 * ================================================================ */

#ifndef GPU_HYPO_SMALL_CUH
#define GPU_HYPO_SMALL_CUH

/* __constant__ definitions moved to main.cu */
/* CHECK_RET macro must match between main.cu and kernels_code.cu */
/* For this build, kernels_code.cu has its own CHK macro */

/* Nothing needed here anymore */

#endif
